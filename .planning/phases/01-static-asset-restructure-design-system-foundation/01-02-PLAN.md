---
phase: 01-static-asset-restructure-design-system-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - app/static/js/tabs/search.js
  - app/static/js/tabs/signals.js
  - app/static/js/tabs/notes.js
  - app/static/js/tabs/reports.js
  - app/templates/index.html
autonomous: false
requirements: [STYLE-01]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All 5 tabs (Watchlist, Semantic Search, News Signals, Research Notes, Agent Reports) render with the same modern SaaS-dashboard visual design established in Plan 01 — Search/Signals/Notes/Reports are now fully interactive through the new file split, not just visually restyled shells."
    - "All 7 onclick-bound actions (addSymbol, removeSymbol, showDetail, runSearch, loadSignals, saveNote, delNote) plus the 2 addEventListener Enter-key shortcuts (#new-symbol, #q) work exactly as before — verified against the live deployed Databricks App per D-02, not just local dev."
    - "A hard-refresh/incognito load of the live app confirms the cache-busted static assets (?v=1 on every CSS/JS URL) load with HTTP 200, not a stale cached version from before this phase's deploy."
    - "Zero backend diff: git diff --stat app/app.py app/lakebase.py app/massive_client.py mcp_server/ pipeline/ sql/ is empty — only app/templates/index.html and new files under app/static/ changed across both plans in this phase."
    - statement: "Untruncated note_text (Research Notes tab) and analysis_report answer/citations text (Agent Reports tab) wraps inside the .note card without overflowing, verified visually on the live app now that these tabs are functional."
      verification: backstop
  artifacts:
    - app/static/js/tabs/search.js
    - app/static/js/tabs/signals.js
    - app/static/js/tabs/notes.js
    - app/static/js/tabs/reports.js
    - app/templates/index.html (modified: remaining 4 script tags added)
  key_links:
    - "index.html script tags now include tabs/search.js, tabs/signals.js, tabs/notes.js, tabs/reports.js inserted between tabs/watchlist.js and main.js — completing the dependency order api.js -> components/*.js -> tabs/*.js (all 5) -> main.js"
    - "main.js tab-switch handler's loadSignals()/loadNotes()/loadReports() calls -> now-defined global functions in tabs/signals.js, tabs/notes.js, tabs/reports.js (previously undefined, deferred from Plan 01)"
    - "main.js's #q keydown addEventListener -> runSearch() now defined in tabs/search.js (previously undefined, deferred from Plan 01)"
    - "onclick=\"runSearch()\" / \"loadSignals()\" / \"saveNote()\" / \"delNote(...)\" (index.html markup, unchanged since before Plan 01) -> window-global functions now defined in tabs/search.js, tabs/signals.js, tabs/notes.js"
---

<objective>
Complete the Phase 1 file split by porting the remaining 4 tabs (Semantic Search, News Signals, Research Notes, Agent Reports) into `app/static/js/tabs/{search,signals,notes,reports}.js`, wiring their `<script src>` tags into `app/templates/index.html`, then running the full DOM/onclick contract + zero-backend-diff verification, and finally the D-02 live-deployed-app manual walkthrough that closes out Phase 1. This expands the tracer slice proven in Plan 01 (classic-script global scope, token-based CSS) out to the rest of the page — no new architecture, purely mechanical replication of Plan 01's already-proven pattern.

After this plan, `app/templates/index.html`'s inline `<style>`/`<script>` blocks are gone entirely, all 10 JS files load in the exact dependency order documented in RESEARCH.md/PATTERNS.md, and all 3 CSS files are wired — completing STYLE-01 and the structural foundation Phases 2-4 build on.

Purpose: Finish the mechanical port started in Plan 01, then prove — via the D-02 live walkthrough — that zero functional regression actually held across a real Databricks Apps deploy, not just local file inspection.
Output: `app/static/js/tabs/{search,signals,notes,reports}.js`, a fully-wired `app/templates/index.html`, and a completed D-02 live verification.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-static-asset-restructure-design-system-foundation/01-CONTEXT.md
@.planning/phases/01-static-asset-restructure-design-system-foundation/01-RESEARCH.md
@.planning/phases/01-static-asset-restructure-design-system-foundation/01-PATTERNS.md
@.planning/phases/01-static-asset-restructure-design-system-foundation/01-UI-SPEC.md
@app/templates/index.html
@.planning/phases/01-static-asset-restructure-design-system-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Port the remaining 4 tabs (search, signals, notes, reports) and wire their script tags</name>
  <files>app/static/js/tabs/search.js, app/static/js/tabs/signals.js, app/static/js/tabs/notes.js, app/static/js/tabs/reports.js, app/templates/index.html</files>
  <read_first>app/templates/index.html, app/static/js/tabs/watchlist.js, app/static/js/components/table.js, app/static/js/components/tag.js, app/static/js/components/states.js, .planning/phases/01-static-asset-restructure-design-system-foundation/01-PATTERNS.md, .planning/phases/01-static-asset-restructure-design-system-foundation/01-UI-SPEC.md</read_first>
  <precondition>app/static/js/{api.js, components/table.js, components/tag.js, components/states.js, tabs/watchlist.js, main.js} and the head/partial-script rewrite of app/templates/index.html from Plan 01 already exist and match Plan 01's SUMMARY.</precondition>
  <action>
Follow the exact same porting pattern established in Plan 01's tabs/watchlist.js — mechanical port, reusing renderTable()/tag()/trendTag()/emptyState()/errState() from components/*.js and esc()/pct()/num()/cls()/api() from api.js, no new abstractions, no logic changes.

app/static/js/tabs/search.js: port runSearch() verbatim from index.html:279-295. Read $('#q').value.trim() and $('#q-ticker').value.trim(); if query empty, showErr('#q-err', 'Enter a search query.'); set $('#q-body').innerHTML to the locked in-flight copy 'Embedding query and searching…' before the await; POST to /api/search via api() with {query, ticker}; on empty result use emptyState with the locked copy 'No matches. Run the pipeline to populate embeddings.'; otherwise map each hit to a '.hit' div (ticker + similarity num(r.similarity,3) + optional sentiment tag(...) span + optional published date + title as a link when r.article_url is present else plain esc(r.title) + a '.txt' div with esc(r.chunk_text.slice(0,420)) + an ellipsis) and join; on error use errState in the catch block writing into #q-body.

app/static/js/tabs/signals.js: port loadSignals() verbatim from index.html:297-314. Read $('#sig-ticker').value.trim(); set #sig-body to the locked in-flight copy 'Loading…' before the await; GET /api/signals with an optional ?ticker= query param via api(); on empty use emptyState with locked copy 'No signals yet — run the pipeline.'; otherwise call renderTable with a header row (Ticker, Date, Headline, Move(num), Vol z(num), Signal) and rows.map producing a '.sym' ticker cell, a mono-styled date cell, a headline cell, a cls(r.daily_return)-classed move cell via pct(), a volume-zscore cell via num(), and a signal-strength tag span using esc(r.signal_strength) as both the class-kind and the text (matching index.html:311's literal pattern, not trendTag — signal_strength values are not up/down/flat); on error use errState into #sig-body.

app/static/js/tabs/notes.js: port loadNotes(), saveNote(), delNote(id) verbatim from index.html:316-345. loadNotes(): GET /api/notes; empty -> emptyState with locked copy 'No notes yet.'; otherwise map each row to a '.note' div with ticker, an author tag span (esc(r.author) as both kind and text, matching index.html:322 — not the tag()/trendTag() author-kind mapping used elsewhere), formatted created_at, a delete link with onclick="delNote(${r.note_id});return false", and the note text; error -> errState into #notes-body. saveNote(): read $('#note-ticker').value.trim() and $('#note-text').value.trim(); validate both non-empty via showErr('#note-err', ...) with the exact locked messages 'Pick a ticker for this note.' and 'Write something before saving.'; POST /api/notes with {ticker, note_text}; clear the textarea; call loadNotes(). delNote(id): native confirm('Delete this note?') unchanged; DELETE /api/notes/{id} via api(); call loadNotes(); catch writes to showErr('#note-err', ...).

app/static/js/tabs/reports.js: port loadReports() verbatim from index.html:347-360. GET /api/reports; empty -> emptyState with locked copy 'No agent analyses saved yet. Ask the agent a question and have it call save_analysis_report.'; otherwise map each row to a '.note' div with ticker, esc(r.model_name || 'agent'), formatted created_at, the question in bold, the answer, and — only if r.citations is a non-empty array — a meta div joining esc(String(c)) for each citation; error -> errState into #rep-body.

app/templates/index.html: insert 4 new `<script src="{{ url_for('static', filename='js/tabs/search.js') }}?v=1"></script>` (and the same pattern for signals.js, notes.js, reports.js) tags between the existing `tabs/watchlist.js` tag and the `main.js` tag, in that exact order (search, signals, notes, reports), so the final end-of-body script order is: api.js, components/table.js, components/tag.js, components/states.js, tabs/watchlist.js, tabs/search.js, tabs/signals.js, tabs/notes.js, tabs/reports.js, main.js (main.js still last). Do not touch any other part of the file — no id/class/data-view/onclick markup changes, matching D-07.
  </action>
  <verify>
    <automated>cd "app" && test -f static/js/tabs/search.js && test -f static/js/tabs/signals.js && test -f static/js/tabs/notes.js && test -f static/js/tabs/reports.js && grep -c "function runSearch" static/js/tabs/search.js | grep -q 1 && grep -c "function loadSignals" static/js/tabs/signals.js | grep -q 1 && grep -c "function loadNotes\|function saveNote\|function delNote" static/js/tabs/notes.js | wc -l | grep -q 3 && grep -c "function loadReports" static/js/tabs/reports.js | grep -q 1 && grep -c "js/tabs/search.js\|js/tabs/signals.js\|js/tabs/notes.js\|js/tabs/reports.js" templates/index.html | grep -q 4 && echo "TABS_WIRED"</automated>
  </verify>
  <done>All 4 remaining tabs.js files exist with their verbatim-ported functions; index.html's end-of-body script block loads all 10 JS files in the exact dependency order (api -> components x3 -> tabs x5 -> main); every onclick handler in the rendered page now resolves against a defined global function.</done>
  <acceptance_criteria>
    - `grep -c 'function runSearch\|function loadSignals\|function loadNotes\|function saveNote\|function delNote\|function loadReports' app/static/js/tabs/search.js app/static/js/tabs/signals.js app/static/js/tabs/notes.js app/static/js/tabs/reports.js` sums to 6 across the 4 files.
    - The script tag order in `app/templates/index.html`, read top to bottom, is exactly: api.js, components/table.js, components/tag.js, components/states.js, tabs/watchlist.js, tabs/search.js, tabs/signals.js, tabs/notes.js, tabs/reports.js, main.js.
    - No `.js` file in this task wraps its content in an IIFE or uses an ES-module script attribute (same discipline as Plan 01, D-01).
    - Every locked empty-state string from UI-SPEC.md's Copywriting Contract (search pre-query/no-matches, signals, notes, reports) appears verbatim (byte-for-byte) in its corresponding new file.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Full contract + zero-backend-diff verification</name>
  <files>app/templates/index.html</files>
  <read_first>.planning/phases/01-static-asset-restructure-design-system-foundation/01-RESEARCH.md</read_first>
  <precondition>Task 1's script-tag wiring is complete.</precondition>
  <action>
Re-run the same three grep-diff checks from Plan 01's Task 2 (id, onclick, data-view) against git HEAD's original index.html — they must still be clean, since Task 1 only added script tags, never touched tab-panel markup. Additionally, verify all 7 onclick-bound global function names (addSymbol, removeSymbol, showDetail, runSearch, loadSignals, saveNote, delNote) now each resolve to exactly one top-level `function name(` declaration somewhere under app/static/js/, confirming Plan 01's deferred functions (runSearch, loadSignals, saveNote, delNote) are no longer missing. Finally, run the phase's zero-backend-diff check from ROADMAP.md's Phase 1 Success Criterion 5 to confirm no backend file was touched across both plans in this phase.
  </action>
  <verify>
    <automated>cd "app" && diff <(git show HEAD:app/templates/index.html | grep -oE 'id="[a-zA-Z0-9_-]+"' | sort -u) <(grep -oE 'id="[a-zA-Z0-9_-]+"' templates/index.html | sort -u) && diff <(git show HEAD:app/templates/index.html | grep -oE 'onclick="[a-zA-Z]+' | sort -u) <(grep -oE 'onclick="[a-zA-Z]+' templates/index.html | sort -u) && diff <(git show HEAD:app/templates/index.html | grep -oE 'data-view="[a-zA-Z]+"' | sort -u) <(grep -oE 'data-view="[a-zA-Z]+"' templates/index.html | sort -u) && for fn in addSymbol removeSymbol showDetail runSearch loadSignals saveNote delNote; do grep -rl "function $fn(" static/js/ >/dev/null || { echo "MISSING $fn"; exit 1; }; done && cd .. && git diff --stat app/app.py app/lakebase.py app/massive_client.py mcp_server/ pipeline/ sql/ | grep -q . && echo "BACKEND DIFF NOT EMPTY - FAIL" || echo "CONTRACT_AND_ZERO_DIFF_CLEAN"</automated>
  </verify>
  <done>DOM/onclick/data-view contract is clean, all 7 handler functions are defined exactly once, and `git diff --stat app/app.py app/lakebase.py app/massive_client.py mcp_server/ pipeline/ sql/` produces empty output.</done>
  <acceptance_criteria>
    - All three grep-diffs (id, onclick, data-view) against `git show HEAD:app/templates/index.html` are empty.
    - Each of the 7 onclick-bound function names resolves to exactly one `function name(` declaration under `app/static/js/`.
    - `git diff --stat app/app.py app/lakebase.py app/massive_client.py mcp_server/ pipeline/ sql/` is empty (Phase 1 Success Criterion 5, ROADMAP.md).
    - `git status --porcelain app/app.py app/lakebase.py app/massive_client.py mcp_server/ pipeline/ sql/` shows no modified/staged/untracked entries.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: D-02 live deployed Databricks App walkthrough</name>
  <precondition>Databricks CLI is configured with valid workspace auth and targets the existing `market-copilot-app` deployment (per README.md's runbook: `databricks sync app /Workspace/Users/&lt;you&gt;/market-copilot-app` then `databricks apps deploy market-copilot --source-code-path /Workspace/Users/&lt;you&gt;/market-copilot-app`) — this is existing, already-configured deployment infrastructure for this live app, not new setup.</precondition>
  <what-built>
The full Phase 1 restructure is complete locally: `app/templates/index.html` now links 3 external CSS files and loads 10 external JS files (all classic, non-module scripts) instead of one inline `<style>`/`<script>` block, restyled with the SaaS-dashboard design-token system across the page shell and all 5 tabs, with zero DOM ID/class/`data-*` changes and zero backend diff (both verified automatically in Task 2).
  </what-built>
  <how-to-verify>
1. Deploy the updated `app/` directory to the live Databricks App: `databricks sync app /Workspace/Users/&lt;you&gt;/market-copilot-app` then `databricks apps deploy market-copilot --source-code-path /Workspace/Users/&lt;you&gt;/market-copilot-app` (per README.md).
2. Open the live app URL in an incognito/private browser window (forces a fresh, non-cached load) and hard-refresh.
3. Confirm the visual design has changed: card-based `.strip` (KPI tiles) and `.panel` sections with shadows, `--accent` blue primary buttons, consolidated typography — not the old flat bordered-table look.
4. Confirm every asset loads with HTTP 200 (browser DevTools Network tab): `css/tokens.css?v=1`, `css/layout.css?v=1`, `css/components.css?v=1`, and all 10 `js/*.js?v=1` files — no 404s, no stale cached versions.
5. Walk through all 7 onclick-bound actions plus the 2 keyboard shortcuts, using RESEARCH.md PITFALLS.md's "Looks Done But Isn't" checklist as the basis:
   - Watchlist: add a ticker symbol (addSymbol), press Enter in the symbol field instead of clicking (keydown shortcut), click a ticker symbol to show its detail table (showDetail), remove a ticker (removeSymbol, confirms via native dialog).
   - Semantic Search: type a query and click Search (runSearch), then press Enter in the query field instead of clicking (keydown shortcut).
   - News Signals: click Apply with and without a ticker filter (loadSignals).
   - Research Notes: save a note (saveNote), delete a note (delNote, confirms via native dialog).
   - Agent Reports: confirm the tab loads and displays existing saved reports (loadReports, triggered by tab click).
6. Confirm the browser console shows zero `Uncaught ReferenceError` on any of the above actions.
7. Confirm long note/report text wraps inside its card rather than overflowing (if a long note/report exists in the data).
  </how-to-verify>
  <resume-signal>Type "approved" once all 7 actions + 2 shortcuts work on the live deployed app with zero console errors and cache-busted assets confirmed, or describe the specific issue found (which action, what happened, any console error text).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser <-> Flask `/api/*` (unchanged) | Third-party-sourced strings now cross this boundary through 4 additional render paths this plan creates: search-result headlines/chunk text (explicitly untrusted, sourced from the Massive news API), signal headlines, user-authored note text, agent-authored report answers/citations |
| Live Databricks App deploy (Task 3) | The D-02 checkpoint is the first time this phase's changes are exercised against the real deployed environment, not local files — a genuinely new verification surface for this phase, not a new attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering / Information Disclosure | `tabs/search.js` (headline titles + chunk text from the third-party Massive news API, explicitly untrusted per RESEARCH.md), `tabs/signals.js`, `tabs/notes.js` (user-authored `note_text`), `tabs/reports.js` (agent-authored `answer`/`citations`) interpolating API-sourced strings into `innerHTML` | high | mitigate | Same `esc()` discipline as Plan 01's T-01-01, applied to every render function in these 4 new files — verified via Task 1's acceptance criteria requiring every locked copy string to appear verbatim (confirming no accidental restructuring of the interpolation pattern) plus a manual read-through of each `${...}` interpolation before commit. |
| T-02-02 | Repudiation | Destructive actions `removeSymbol` (Plan 01) and `delNote` (this plan) rely on native `confirm()` dialogs, unchanged from today | low | accept | Behavior/copy unchanged this phase per UI-SPEC.md's Copywriting Contract — a custom confirmation modal is outside D-07's additive-markup-only scope; native `confirm()` already requires explicit user confirmation before either DELETE call. |
| T-02-03 | (no new attack surface, whole-phase reiteration) | Entire Phase 1 scope | low | accept | Zero backend diff is the primary mitigation, enforced by Task 2's automated `git diff --stat` check — this phase adds no new Flask routes, no new auth logic, no new input surface across either plan; it exclusively relocates and restyles existing frontend code against the existing, unchanged `/api/*` JSON contract. The D-02 live-deploy checkpoint (Task 3) confirms this holds true in the real deployed environment, not just locally. |

</threat_model>

<verification>
- All 4 remaining `tabs/*.js` files exist with their verbatim-ported functions, reusing Plan 01's shared `components/*.js`/`api.js` helpers.
- `app/templates/index.html`'s script block loads all 10 JS files in the documented dependency order; no inline `<style>`/`<script>` remains anywhere.
- The DOM/onclick/`data-view` contract diff against `git show HEAD:app/templates/index.html` is clean.
- `git diff --stat app/app.py app/lakebase.py app/massive_client.py mcp_server/ pipeline/ sql/` is empty.
- D-02's live deployed Databricks App walkthrough is approved (Task 3 checkpoint).
</verification>

<success_criteria>
Phase 1 is complete when: the page shell and all 5 tabs render the modern SaaS-dashboard design-token system; all 7 onclick-bound actions plus 2 keyboard shortcuts work identically to before, confirmed live; CSS/JS load via cache-busted `app/static/` URLs confirmed with a live hard-refresh/incognito check; the DOM ID/class/`data-*` contract is provably unchanged; and zero backend files were touched. This satisfies ROADMAP.md's Phase 1 Success Criteria 1-5 and REQUIREMENTS.md's STYLE-01, and establishes the render-function scaffolding (`components/table.js`, `tag.js`, `states.js`) Phases 2-4 extend.
</success_criteria>

<output>
Create `.planning/phases/01-static-asset-restructure-design-system-foundation/01-02-SUMMARY.md` when done
</output>
