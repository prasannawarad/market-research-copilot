---
phase: 01-static-asset-restructure-design-system-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/static/css/tokens.css
  - app/static/css/layout.css
  - app/static/css/components.css
  - app/static/js/api.js
  - app/static/js/components/table.js
  - app/static/js/components/tag.js
  - app/static/js/components/states.js
  - app/static/js/tabs/watchlist.js
  - app/static/js/main.js
  - app/templates/index.html
autonomous: true
requirements: [STYLE-01]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Page shell (KPI stat-tile strip #strip, tab nav .tabs) and the Watchlist tab (#v-watchlist) render using the new CSS design-token system — .strip>div and .panel get border-radius:var(--radius-md) + box-shadow:var(--shadow-sm) card treatment, buttons default to var(--accent) background, typography consolidates to the 4-role scale (Body 15px/400, Label 10px/600, Heading 12px/600, Display 24px/600) — a clear step up from the flat bordered-table look, while all Watchlist data/content stays present and correctly labeled."
    - "Add ticker (addSymbol), remove ticker (removeSymbol), and show ticker detail (showDetail) all still work on the Watchlist tab — bound to the same onclick=\"addSymbol()\" / onclick=\"showDetail('...')\" / onclick=\"removeSymbol('...')\" attributes, now resolving against top-level global functions defined in app/static/js/tabs/watchlist.js (D-01: classic non-module, non-IIFE <script src> tags, not addEventListener rewiring)."
    - "CSS and JS load from app/static/css/ and app/static/js/ via {{ url_for('static', filename=...) }} with a literal ?v=1 cache-busting query string on every new <link> and <script src> tag (not sourced from app.py/Flask config)."
    - "No DOM ID, class, or data-* attribute in app/templates/index.html has changed — grep -oE 'id=\"[a-zA-Z0-9_-]+\"' / 'onclick=\"[a-zA-Z]+' / 'data-view=\"[a-zA-Z]+\"' before (git HEAD) and after this plan produce identical sorted output, since only <head> and the end-of-<body> script block change; all 5 tab panels' markup (including the 4 tabs whose JS is not yet split — Search, Signals, Notes, Reports) is left byte-for-byte untouched this plan."
    - "The old inline <style>...</style> and <script>...</script> blocks (index.html:7-75, 177-367 before this plan) are fully removed from index.html, replaced entirely by external <link>/<script src> references."
    - statement: "Long, untruncated text inside .note and .hit .txt (note_text, report answer/citations, search chunk_text) does not visually overflow the new bordered/shadowed card container — word-wrap:break-word;overflow-wrap:anywhere is present on both selectors in components.css."
      verification: backstop
  artifacts:
    - app/static/css/tokens.css
    - app/static/css/layout.css
    - app/static/css/components.css
    - app/static/js/api.js
    - app/static/js/components/table.js
    - app/static/js/components/tag.js
    - app/static/js/components/states.js
    - app/static/js/tabs/watchlist.js
    - app/static/js/main.js
    - app/templates/index.html (modified: head links + partial script block)
  key_links:
    - "index.html <head> <link> tags -> app/static/css/{tokens,layout,components}.css, cache-busted ?v=1, dependency order tokens -> layout -> components"
    - "index.html end-of-<body> <script src> tags -> api.js -> components/table.js -> components/tag.js -> components/states.js -> tabs/watchlist.js -> main.js (main.js MUST be last; its top-level loadStats()/loadWatchlist() calls run synchronously the instant its tag is parsed)"
    - "onclick=\"addSymbol()\" / onclick=\"showDetail('...')\" / onclick=\"removeSymbol('...')\" (index.html markup, unchanged) -> window-global functions in tabs/watchlist.js (classic script global scope, no type=\"module\" attribute, no wrapping IIFE)"
    - "components/tag.js tag()/trendTag() -> components.css .tag.positive/.tag.negative/.tag.neutral modifier classes (class names unchanged from index.html:61-63)"
    - "api.js esc() -> every API-sourced string interpolated into innerHTML across table.js/tabs/watchlist.js (r.symbol, r.trend, r.bar_date) — same escaping discipline as the current inline script"
---

<objective>
Relocate the current inline `<style>`/`<script>` block in `app/templates/index.html` into the modular `app/static/css/{tokens,layout,components}.css` + `app/static/js/{api,components/*,tabs/*,main}.js` structure, and apply the modern SaaS-dashboard visual design (D-03 through D-08) to the page shell and the Watchlist tab — the app's default-active tab — wired fully end-to-end (CSS + shared JS + Watchlist JS + template) so a real user can add/remove/inspect a ticker through the newly-split, newly-restyled files. This is the tracer slice for Phase 1: it proves the classic-script (non-module, non-IIFE) loading pattern locked by D-01 does not break the `onclick` contract, and proves the token-based CSS system renders the card-based look, before Plan 02 expands the same pattern to the remaining 4 tabs.

Per D-07, this plan changes ZERO existing DOM ID/class/`data-*` value — only `<head>` and the end-of-`<body>` script block of `index.html` are touched; all 5 tab panels' markup (including Search/Signals/Notes/Reports, whose JS is not split until Plan 02) is left byte-for-byte unchanged.

**Reviewed and dismissed (not silently dropped) — UI-SPEC.md "UI Considerations" not-applicable rows:** KPI-strip partial/zero-one-many (single atomic `/api/stats` response, fixed 6-tile set), data-table long-text (short cell content only), hit/note/report-card partial (atomic per-card fetch), form-input empty/partial (standard browser behavior). None require action this phase.

**Explicit planner assumption (UI-SPEC.md unresolved row — "loading, Form submit buttons"):** No submit button (`addSymbol`/`runSearch`/`saveNote`/`loadSignals`) disables itself or shows an in-flight spinner during its `fetch()` call, today or after this plan — this is unchanged behavior, not a regression, and out of STYLE-01's restyle-only scope. A low-cost additive fix (`button.disabled = true` around the `await`) is a reasonable Phase 2+ candidate, not a Phase 1 requirement. Do not add this now.

Purpose: Establish the foundation every later phase (2-4) builds on, and prove the two riskiest mechanics (classic-script global scope, DOM-ID/class-preserving restructure) work before scaling to the rest of the page.
Output: `app/static/css/{tokens,layout,components}.css`, `app/static/js/{api,main}.js`, `app/static/js/components/{table,tag,states}.js`, `app/static/js/tabs/watchlist.js`, and a rewritten `app/templates/index.html` head + partial script block.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-static-asset-restructure-design-system-foundation/01-CONTEXT.md
@.planning/phases/01-static-asset-restructure-design-system-foundation/01-RESEARCH.md
@.planning/phases/01-static-asset-restructure-design-system-foundation/01-PATTERNS.md
@.planning/phases/01-static-asset-restructure-design-system-foundation/01-UI-SPEC.md
@app/templates/index.html
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: CSS design-token system + shared JS scaffolding + Watchlist tab, wired end-to-end</name>
  <files>app/static/css/tokens.css, app/static/css/layout.css, app/static/css/components.css, app/static/js/api.js, app/static/js/components/table.js, app/static/js/components/tag.js, app/static/js/components/states.js, app/static/js/tabs/watchlist.js, app/static/js/main.js, app/templates/index.html</files>
  <read_first>app/templates/index.html, .planning/phases/01-static-asset-restructure-design-system-foundation/01-PATTERNS.md, .planning/phases/01-static-asset-restructure-design-system-foundation/01-UI-SPEC.md, .planning/phases/01-static-asset-restructure-design-system-foundation/01-RESEARCH.md</read_first>
  <reversibility rating="costly">D-01 locks classic (non-module) <script src> loading for the whole JS split, established by this task. Switching to ES modules later requires a coordinated rewrite of every onclick="..." attribute across index.html to addEventListener — costly to reverse, per CONTEXT.md.</reversibility>
  <action>
Build the CSS design-token system first, then the shared JS layer, then the Watchlist tab, then rewire index.html — in that order, since later files depend on earlier ones' tokens/helpers.

CSS — app/static/css/tokens.css: define a single :root block. Preserve verbatim (do not rename) the 12 existing custom properties from index.html:8-14 — ink, soft, muted, paper, surface, rule, up, down, flat, accent (#1d6fe0, kept as-is per UI-SPEC.md's resolved Open Question — do not shift toward purple), mono, sans. Add the new spacing/radius/shadow tokens exactly as specified in UI-SPEC.md's Spacing Scale and Radius & Elevation tables: space-1 through space-16 (4,8,12,16,24,32,48,64px), radius-sm (4px), radius-md (8px), radius-lg (14px, unused this phase), shadow-sm (0 1px 2px rgba(19,26,36,.06)), shadow-md (0 2px 8px rgba(19,26,36,.08)). Do not add an accent-soft token — UI-SPEC.md's final Color section does not define one.

CSS — app/static/css/layout.css: relocate index.html:15-39 and :73-74 (the box-sizing reset, body, .shell, .mast, .wordmark, .who, .strip, .strip div rules, .tabs, .tab/.tab.on, .panel, h2, and the @media(max-width:760px) block) verbatim as a starting point, then apply these UI-SPEC.md-specified value changes only (no selector renames): body padding becomes 0 20px var(--space-16) (replacing the non-token 0 20px 80px). .strip becomes a card grid per UI-SPEC/RESEARCH Pattern 5 — grid-template-columns:repeat(auto-fit, minmax(140px, 1fr)); gap:var(--space-3); and .strip > div gets background:var(--surface); border:1px solid var(--rule); border-radius:var(--radius-md); box-shadow:var(--shadow-sm); padding:var(--space-4) (replacing the old border-left divider + padding:14px 0 rule). .strip .n becomes the Display typography role: font-size 24px, font-weight 600, line-height 1.2, font-variant-numeric tabular-nums (was 22px). .strip .k and .who become the Label role: font-size 10px, font-weight 600, line-height 1.2 (was 9px/11px). .tab/.tab.on and .wordmark and h2 become the Heading role: font-size 12px, font-weight 600, line-height 1.2 (was 11-13px); .tab/.tab.on also gets border-radius:var(--radius-sm) (was 2px). .panel gets border-radius:var(--radius-md); box-shadow:var(--shadow-sm) added (border:1px solid var(--rule) kept, not replaced). Preserve the @media(max-width:760px) breakpoint content and target selectors unchanged (only .strip's base grid-template-columns value inside it may need re-checking against the new auto-fit rule — keep the 3-column mobile fallback if auto-fit doesn't already degrade acceptably at 760px).

CSS — app/static/css/components.css: relocate index.html:40-71 (input/select/textarea, button, button.ghost, button.x, table/th/td/td.num,th.num, .sym, .sym:hover, .up/.down/.flat, .tag + all 8 .tag.* modifiers, .hit + .hit .meta + .hit .txt + .hit a, .note + .note .meta, .empty/.err, .hide) verbatim as a starting point, then apply these UI-SPEC.md-specified changes only (no selector/modifier-class renames — .tag.strong/.material/.routine/.positive/.negative/.neutral/.agent/.human must keep their exact names, referenced from components/tag.js): input/select/textarea get border-radius:var(--radius-sm) (was 2px) and a new :focus rule — border-color:var(--accent); outline:2px solid var(--accent); outline-offset:1px (this selector/state did not exist before, additive only). button (bare, default) switches background and border-color from var(--ink) to var(--accent) (color:var(--paper) unchanged) and border-radius:var(--radius-sm); button.ghost and button.x keep their existing background/color/border-color values unchanged, only border-radius becomes var(--radius-sm). th and .tag and .hit .meta and .note .meta become the Label role: font-size 10px, font-weight 600, line-height 1.2 (was 9-10px) — .tag also gains border-radius:var(--radius-sm) added to its base rule (was 2px). Add word-wrap:break-word;overflow-wrap:anywhere to .note and to .hit .txt (new rule this phase — the untruncated-text-overflow backstop from UI-SPEC.md's UI Considerations). .hide, .empty, .err, .sym, .sym:hover, .up/.down/.flat, table/td/td.num,th.num stay exactly as today (no value or selector change).

JS — app/static/js/api.js (loaded first, no deps): port verbatim from index.html:178-189 the five helper consts ($, pct, num, esc, cls) and the async api(url, opts) fetch wrapper. These must be plain top-level const/function declarations — no IIFE wrapper, no ES-module script attribute — so every other file can reference them as globals.

JS — app/static/js/components/table.js: define one generic function renderTable(headerHtml, bodyRowsHtml) that returns the string '<table><thead><tr>' + headerHtml + '</tr></thead><tbody>' + bodyRowsHtml + '</tbody></table>' — a shared table-shell wrapper (RESEARCH.md Anti-Pattern: don't duplicate this markup per tab). Each tabs/*.js file supplies its own header-cell HTML and its own rows.map(...).join('') row markup, calling renderTable with those two strings — row-shape logic (which columns, which esc()/pct()/num() calls) stays local to each tabs file, not generalized into table.js.

JS — app/static/js/components/tag.js: port verbatim the two functions from PATTERNS.md's Pattern 4/Shared Patterns example — tag(value, kind) returning '<span class="tag ' + kind + '">' + esc(value) + '</span>', and trendTag(trend) that maps trend 'up'/'down'/other to kind 'positive'/'negative'/'neutral' and calls tag(trend || 'n/a', kind). Modifier class names strong/material/routine/positive/negative/neutral/agent/human are locked (components.css already defines them) — do not introduce new modifier names.

JS — app/static/js/components/states.js: port verbatim showErr(id, msg) from index.html:190-193 (querySelector the id, set textContent, remove 'hide', setTimeout 6000ms to re-add 'hide'). Add two small helpers: emptyState(msg) returning '<div class="empty">' + msg + '</div>', and errState(err) returning '<div class="err">' + esc(err.message) + '</div>' — both take the message/error as a parameter, never hardcode copy (each tabs/*.js call site supplies its own UI-SPEC.md-locked copy string).

JS — app/static/js/tabs/watchlist.js: port verbatim (mechanical port, no logic changes) loadWatchlist(), addSymbol(), removeSymbol(symbol), showDetail(ticker) from index.html:217-277, rewriting each table-building template literal to call renderTable(headerHtml, rows.map(...).join('')) from table.js and each <span class="tag ...> trend cell to call trendTag(r.trend) from tag.js, and each empty/error branch to call emptyState(...)/errState(...) from states.js where the original used a literal '<div class="empty">...' or '<div class="err">...' string. Preserve every onclick="showDetail('${esc(r.symbol)}')" and onclick="removeSymbol('${esc(r.symbol)}')" attribute string exactly, and preserve the exact locked empty-state copy "No tickers yet. Add one above, then run the Spark pipeline." (UI-SPEC.md Copywriting Contract) and "No metrics yet — run the pipeline." for showDetail's empty branch.

JS — app/static/js/main.js (loaded LAST): port verbatim from index.html:195-216 the full tab-switch wiring — document.querySelectorAll('.tab').forEach(...) that toggles the 'on' class and toggles 'hide' on all 5 #v-* panels via the ['watchlist','search','signals','notes','reports'] array, and conditionally calls loadSignals()/loadNotes()/loadReports() when that tab is selected. Also port loadStats() (index.html:205-215) and, from index.html:362-366, both addEventListener bindings (#new-symbol keydown->addSymbol(), #q keydown->runSearch()) and the two boot calls loadStats(); loadWatchlist();. Note: loadSignals/loadNotes/loadReports/runSearch are not yet defined (their files are Plan 02's job) — main.js still references their names in the tab-switch conditional and the #q keydown listener; this is safe at load time (these are function-body references, not top-level calls) and will only throw if a user clicks Search/Signals/Notes/Reports or presses Enter in #q before Plan 02 completes, which is expected and acceptable mid-phase. loadStats() and loadWatchlist() (the two boot calls) DO run immediately and MUST both already be defined when main.js's tag is reached — confirm tabs/watchlist.js loads before main.js in index.html.

Template — app/templates/index.html: remove the entire inline <style>...</style> block (lines 7-75) and replace it in <head> with three <link rel="stylesheet"> tags pointing at {{ url_for('static', filename='css/tokens.css') }}?v=1, then css/layout.css?v=1, then css/components.css?v=1, in that order. Remove the entire inline <script>...</script> block (lines 177-367) and replace it, at the same end-of-<body> position, with six <script src="{{ url_for('static', filename='js/...') }}?v=1"></script> tags in this exact order: js/api.js, js/components/table.js, js/components/tag.js, js/components/states.js, js/tabs/watchlist.js, js/main.js. Do not add script tags for tabs/search.js, tabs/signals.js, tabs/notes.js, or tabs/reports.js yet — those files and their tags are Plan 02's job. Do not touch any markup between </head> and the closing <script> block (the <header class="mast"> through the five #v-* tab panel divs) — every id, class, data-view, and onclick attribute in that region must remain byte-for-byte identical to the current file, including the ones belonging to Search/Signals/Notes/Reports whose JS does not exist yet.
  </action>
  <verify>
    <automated>cd "app" && test -f static/css/tokens.css && test -f static/css/layout.css && test -f static/css/components.css && test -f static/js/api.js && test -f static/js/components/table.js && test -f static/js/components/tag.js && test -f static/js/components/states.js && test -f static/js/tabs/watchlist.js && test -f static/js/main.js && grep -q "css/tokens.css" templates/index.html && grep -q "css/layout.css" templates/index.html && grep -q "css/components.css" templates/index.html && grep -q "js/api.js" templates/index.html && grep -q "js/main.js" templates/index.html && ! grep -q "<style>" templates/index.html && ! grep -qE "^<script>$" templates/index.html && echo "TRACER_OK"</automated>
  </verify>
  <done>index.html's <head> links to the 3 new CSS files and its end-of-body loads the 6 new JS files (api, 3 components, tabs/watchlist, main) in dependency order; no inline <style> or bare <script> block remains; the Watchlist tab (default-active on load) visually shows the card-based design-token system and its 3 onclick actions (add/remove/show-detail) resolve against globals defined in tabs/watchlist.js.</done>
  <acceptance_criteria>
    - All 9 new files listed in <files> exist under app/static/, non-empty.
    - `grep -c 'type="module"' app/templates/index.html` returns 0 — no script tag uses the ES-module attribute (D-01).
    - Each of api.js, components/table.js, components/tag.js, components/states.js, tabs/watchlist.js, main.js contains at least one top-level `function ` or `const ... = ` declaration with no wrapping `(function(){` / `})();` IIFE pattern around the whole file.
    - `grep -c 'function addSymbol\|function removeSymbol\|function showDetail\|function loadWatchlist' app/static/js/tabs/watchlist.js` returns 4.
    - `grep -c '\-\-accent:#1d6fe0' app/static/css/tokens.css` returns 1 (accent token preserved verbatim, not shifted).
    - `grep -c 'overflow-wrap:anywhere' app/static/css/components.css` returns 2 (applied to both .note and .hit .txt).
    - The 12 pre-existing CSS custom property names (--ink, --soft, --muted, --paper, --surface, --rule, --up, --down, --flat, --accent, --mono, --sans) all appear unchanged in tokens.css.
    - Markup between `</head>` and the new end-of-body script block (all 5 `#v-*` tab panels, `#strip`, `.tabs` nav) is unchanged from `git show HEAD:app/templates/index.html` for that same region.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: DOM/onclick/data-view contract verification for the partial-split state</name>
  <files>app/templates/index.html</files>
  <read_first>app/templates/index.html, .planning/phases/01-static-asset-restructure-design-system-foundation/01-RESEARCH.md</read_first>
  <precondition>Task 1's file writes are complete and app/templates/index.html has already been rewritten (head links + partial script block) before this task runs.</precondition>
  <action>
Run the exact three before/after grep-diff commands from RESEARCH.md's "Verified Contract Inventory" against the current index.html and against git HEAD's version of the same file, for: id="..." values, onclick="..." function-name prefixes, and data-view="..." values. Since Task 1 touched only <head> and the end-of-<body> script block (never the tab-panel markup itself, per D-07), all three diffs must be empty — this includes the 4 onclick handlers belonging to tabs not yet split (runSearch, loadSignals, saveNote, delNote), which remain present in the markup as attribute strings even though their backing JS functions do not exist until Plan 02. Confirm the count of distinct onclick function-name prefixes is exactly 7 (addSymbol, removeSymbol, showDetail, runSearch, loadSignals, saveNote, delNote) and the count of distinct id values matches the 29 documented in RESEARCH.md's Verified Contract Inventory table. If any diff is non-empty, stop and report the exact delta — do not proceed to Plan 02 with a broken contract.
  </action>
  <verify>
    <automated>cd "app" && diff <(git show HEAD:app/templates/index.html | grep -oE 'id="[a-zA-Z0-9_-]+"' | sort -u) <(grep -oE 'id="[a-zA-Z0-9_-]+"' templates/index.html | sort -u) && diff <(git show HEAD:app/templates/index.html | grep -oE 'onclick="[a-zA-Z]+' | sort -u) <(grep -oE 'onclick="[a-zA-Z]+' templates/index.html | sort -u) && diff <(git show HEAD:app/templates/index.html | grep -oE 'data-view="[a-zA-Z]+"' | sort -u) <(grep -oE 'data-view="[a-zA-Z]+"' templates/index.html | sort -u) && echo "CONTRACT_CLEAN"</automated>
  </verify>
  <done>All three grep-diffs (id, onclick, data-view) between git HEAD's index.html and the current index.html are empty, confirming zero DOM contract drift from Task 1's head/script-block-only edit.</done>
  <acceptance_criteria>
    - `diff` exit code 0 for all three grep comparisons (id list, onclick list, data-view list).
    - Distinct onclick function-name count is 7.
    - No `.hide`, `.tab`/`.tab.on`, `.err`, `.empty` class token was renamed or removed (spot-check via `grep -c 'class="hide"\|class="err hide"\|class="tab on"' app/templates/index.html` returns at least 1 for each pattern present in HEAD).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser <-> Flask `/api/*` (unchanged) | Third-party- and user-sourced strings (ticker symbol/trend from the Spark pipeline, ultimately Massive-sourced) cross into `innerHTML` via the render functions this plan relocates and splits |
| Static asset request <-> Flask static blueprint (new surface this phase) | `app/static/css/*.css` and `app/static/js/*.js` are now served via Flask's default `/static/<path:filename>` blueprint — zero new route code in `app/app.py` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering / Information Disclosure | `components/table.js`, `components/tag.js`, `tabs/watchlist.js` render functions interpolating API-sourced strings (`r.symbol`, `r.trend`, `r.bar_date`) into `innerHTML` | high | mitigate | Every interpolation of an API-sourced text field routes through `esc()` (ported verbatim into `api.js`), exactly matching the original inline code's pattern — this is the phase's #1 flagged risk (RESEARCH.md: splitting one template-literal block into many raises the chance a call site drops the `esc()` wrap). Verified via Task 1's acceptance criteria (function-declaration checks) plus a manual read-through of every `${...}` interpolation in `tabs/watchlist.js` before commit. |
| T-01-02 | Tampering | Script-loading pattern accidentally scoped via ES-module or IIFE wrapping, un-exposing `addSymbol`/`removeSymbol`/`showDetail` from `window` and silently breaking `onclick` handlers (Pitfall 1) | high | mitigate | Classic `<script src>` tags, no `type="module"` attribute, no wrapping IIFE, per D-01 — enforced by Task 1's acceptance criteria (`grep -c 'type="module"'` = 0, top-level `function`/`const` declaration check) and functionally provable by clicking Add/Remove/Show-detail on the Watchlist tab. |
| T-01-03 | Elevation of Privilege (new attack surface) | New `app/static/` file-serving surface via Flask's default static blueprint | low | accept | No user-supplied path construction, no new route code in `app/app.py` (zero backend diff enforced in Plan 02). This phase's asset-serving surface is materially identical in risk to any Flask app already serving static files — Flask's static blueprint itself is a well-established, unmodified control. |

</threat_model>

<verification>
- All 9 new files under `app/static/` exist with the content described in Task 1's action.
- `app/templates/index.html`'s `<head>` links the 3 new CSS files (cache-busted `?v=1`); its end-of-`<body>` loads the 6 new JS files in dependency order; no inline `<style>`/`<script>` block remains.
- The DOM/onclick/data-view contract diff (Task 2) is clean against `git show HEAD:app/templates/index.html`.
- The Watchlist tab's 3 interactive actions are traceable to top-level global functions in `tabs/watchlist.js` (not module-scoped, not IIFE-wrapped).
</verification>

<success_criteria>
Plan 01 is complete when: the CSS design-token system exists and is wired into `index.html`; `api.js`, the 3 shared `components/*.js` files, `tabs/watchlist.js`, and `main.js` exist and are wired in the correct dependency order; the DOM/onclick/`data-view` contract is provably unchanged; and the Watchlist tab is fully functional end-to-end through the new file structure. Plan 02 depends on this state to add the remaining 4 tabs.
</success_criteria>

<output>
Create `.planning/phases/01-static-asset-restructure-design-system-foundation/01-01-SUMMARY.md` when done
</output>
